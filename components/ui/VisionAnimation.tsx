"use client";

export function VisionAnimation() {
  return (
    <div className="vision-anim">
      {/* SVG image — cards float as one unit */}
      {/* eslint-disable-next-line @next/next/no-img-element */}
      <img
        src="/images/vision-svg.svg"
        alt=""
        className="vision-anim-full-img"
      />
    </div>
  );
}
